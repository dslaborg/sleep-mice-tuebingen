data:
  stage_map:
    Wake: 'Wake'
    REM: 'REM'
    Non REM: 'Non REM'
    Pre REM: 'Non REM'
    Artifact: null

experiment:
  data:
    stages: ['Wake', 'REM', 'Non REM']
    channels: ['EEG_PR']

  training:
    optimizer:
      class: 'Adam'
      l2_weight_decay: 1.e-4

  model:
    name: 'model_3conv_pool'
